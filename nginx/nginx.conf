events {}

http {
    upstream api {
        server backend:8080;
    }

    upstream web {
        server frontend:80;
    }

    server {
        listen 80;

        location /api/ {
            proxy_pass http://api;
        }

        location / {
            proxy_pass http://web;
        }
    }
}